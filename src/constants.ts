import { Format } from './types'

export const FORMAT_CHOICES: ReadonlyArray<Format> = ['camel', 'kebab', 'pascal', 'snake']

export const DEFAULT_EXTENSIONS = ['ts', 'tsx', 'js', 'jsx']

export const EXCLUDED_FOLDERS = ['node_modules', 'dist', 'lib']
